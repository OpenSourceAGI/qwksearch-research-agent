{
  "name": "train-next-word-prediction",
  "private": true,
  "version": "1.0.0",
  "description": "Wrangler/Cloudflare Containers wrapper around the Python transformer training package in src/ and docker/",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.0.13",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.100.0"
  }
}
