/**
 * Entry point for the ImageGif extension that re-exports the extension and its React components. Lets the app import animated GIF images (via Giphy) from a single, stable module path.
 */

export * from './ImageGif';
