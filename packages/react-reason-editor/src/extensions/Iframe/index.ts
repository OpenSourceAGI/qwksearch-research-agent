/**
 * Entry point for the Iframe extension that re-exports the extension and its React components. Lets the app import embedding external content via iframes from a single, stable module path.
 */

export * from './Iframe';
