/**
 * Entry point for the ImportWord extension that re-exports the extension and its React components. Lets the app import importing Word (.docx) documents from a single, stable module path.
 */

export * from './ImportWord';
