/**
 * Entry point for the FontSize extension that re-exports the extension and its React components. Lets the app import font-size selection from a single, stable module path.
 */

export * from './FontSize';
