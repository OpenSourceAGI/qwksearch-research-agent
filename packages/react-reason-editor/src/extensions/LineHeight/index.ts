/**
 * Entry point for the LineHeight extension that re-exports the extension and its React components. Lets the app import line-height/spacing control from a single, stable module path.
 */

export * from './LineHeight';
