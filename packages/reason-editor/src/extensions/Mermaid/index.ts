/**
 * Entry point for the Mermaid extension that re-exports the extension and its React components. Lets the app import Mermaid diagrams from a single, stable module path.
 */

export * from './Mermaid';
