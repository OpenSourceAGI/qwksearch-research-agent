/**
 * Entry point for the HorizontalRule extension that re-exports the extension and its React components. Lets the app import horizontal-rule dividers from a single, stable module path.
 */

export * from './HorizontalRule';
