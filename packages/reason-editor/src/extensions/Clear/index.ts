/**
 * Entry point for the Clear extension that re-exports the extension and its React components. Lets the app import clearing inline formatting from a selection from a single, stable module path.
 */

export * from './Clear';
