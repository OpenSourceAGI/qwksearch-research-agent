/**
 * Entry point for the CodeView extension that re-exports the extension and its React components. Lets the app import viewing and editing the document's raw HTML source from a single, stable module path.
 */

export * from './CodeView';
