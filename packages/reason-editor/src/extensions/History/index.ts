/**
 * Entry point for the History extension that re-exports the extension and its React components. Lets the app import undo/redo history from a single, stable module path.
 */

export * from './History';
