/**
 * Entry point for the Italic extension that re-exports the extension and its React components. Lets the app import italic text formatting from a single, stable module path.
 */

export * from './Italic';
