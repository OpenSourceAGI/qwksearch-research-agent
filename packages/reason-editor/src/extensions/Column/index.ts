/**
 * Entry point for the Column extension that re-exports the extension and its React components. Lets the app import multi-column layouts from a single, stable module path.
 */

export * from './Column';
