/**
 * Entry point for the Bold extension that re-exports the extension and its React components. Lets the app import bold text formatting from a single, stable module path.
 */

export * from './Bold';
