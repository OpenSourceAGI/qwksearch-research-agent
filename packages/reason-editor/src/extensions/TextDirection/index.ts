/**
 * Entry point for the TextDirection extension that re-exports the extension and its React components. Lets the app import text direction (LTR/RTL) from a single, stable module path.
 */

export * from './TextDirection';
