/**
 * Entry point for the SlashCommand extension that re-exports the extension and its React components. Lets the app import the “/” slash-command menu from a single, stable module path.
 */

export * from './SlashCommand';
