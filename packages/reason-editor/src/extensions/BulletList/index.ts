/**
 * Entry point for the BulletList extension that re-exports the extension and its React components. Lets the app import unordered (bullet) lists from a single, stable module path.
 */

export * from './BulletList';
