/**
 * Entry point for the TextAlign extension that re-exports the extension and its React components. Lets the app import text alignment from a single, stable module path.
 */

export * from './TextAlign';
