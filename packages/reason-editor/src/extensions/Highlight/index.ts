/**
 * Entry point for the Highlight extension that re-exports the extension and its React components. Lets the app import text highlighting from a single, stable module path.
 */

export * from './Highlight';
