/**
 * Entry point for the OfficePaste extension that re-exports the extension and its React components. Lets the app import cleaning content pasted from Microsoft Office from a single, stable module path.
 */

export * from './OfficePaste';
