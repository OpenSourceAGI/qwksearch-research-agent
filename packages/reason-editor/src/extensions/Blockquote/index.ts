/**
 * Entry point for the Blockquote extension that re-exports the extension and its React components. Lets the app import blockquote formatting from a single, stable module path.
 */

export * from './Blockquote';
