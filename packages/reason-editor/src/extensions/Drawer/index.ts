/**
 * Entry point for the Drawer extension that re-exports the extension and its React components. Lets the app import hand-drawn sketch/drawing blocks (easydrawer) from a single, stable module path.
 */

export * from './Drawer';
