/**
 * Entry point for the Twitter extension that re-exports the extension and its React components. Lets the app import embedded tweets from a single, stable module path.
 */

export * from './Twitter';
