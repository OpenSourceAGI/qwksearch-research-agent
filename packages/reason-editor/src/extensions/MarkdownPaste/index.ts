/**
 * Entry point for the MarkdownPaste extension that re-exports the extension and its React components. Lets the app import converting pasted Markdown into rich text from a single, stable module path.
 */

export * from './MarkdownPaste';
