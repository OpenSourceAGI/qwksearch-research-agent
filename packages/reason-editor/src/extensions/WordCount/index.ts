/**
 * Entry point for the WordCount extension that re-exports the extension and its React components. Lets the app import word and character counting from a single, stable module path.
 */

export * from './WordCount';
