/**
 * Entry point for the Emoji extension that re-exports the extension and its React components. Lets the app import emoji insertion from a single, stable module path.
 */

export * from './Emoji';
