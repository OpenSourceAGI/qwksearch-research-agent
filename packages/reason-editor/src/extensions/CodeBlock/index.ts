/**
 * Entry point for the CodeBlock extension that re-exports the extension and its React components. Lets the app import syntax-highlighted code blocks from a single, stable module path.
 */

export * from './CodeBlock';
