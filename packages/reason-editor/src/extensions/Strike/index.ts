/**
 * Entry point for the Strike extension that re-exports the extension and its React components. Lets the app import strikethrough text formatting from a single, stable module path.
 */

export * from './Strike';
