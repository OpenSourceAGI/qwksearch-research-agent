/**
 * Entry point for the Heading extension that re-exports the extension and its React components. Lets the app import headings (h1–h6) from a single, stable module path.
 */

export * from './Heading';
