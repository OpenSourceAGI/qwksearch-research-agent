/**
 * Entry point for the ExportWord extension that re-exports the extension and its React components. Lets the app import exporting the document to a Word (.docx) file from a single, stable module path.
 */

export * from './ExportWord';
